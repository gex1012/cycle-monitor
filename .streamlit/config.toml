# Streamlit 配置：深色主题，与图表(plotly_dark)统一观感
[theme]
base = "dark"
primaryColor = "#ff7043"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#1a1d24"

[server]
# 云端由平台接管端口；本地运行时这些为默认值
headless = true

[browser]
gatherUsageStats = false
